import * as mongoose from 'mongoose';
export declare const TaskScheme: mongoose.Schema<any, mongoose.Model<any, any, any, any, any>, {}, {}, any, {}, "type", {
    description?: string;
    completed?: boolean;
}>;
